{
  "name": "sq5-playground",
  "version": "0.1.0",
  "description": "Interactive audio engineering training playground and sandbox for church AV ministry volunteers.",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@foh-sim/web",
    "build": "npm run build --workspaces --if-present",
    "preview": "npm run preview --workspace=@foh-sim/web",
    "test": "npm run test --workspaces --if-present",
    "test:run": "vitest run",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
